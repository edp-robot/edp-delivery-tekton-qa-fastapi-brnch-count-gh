apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-brnch-count-gh.fullname" . }}
  labels:
    {{- include "fastapi-brnch-count-gh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-brnch-count-gh.selectorLabels" . | nindent 4 }}
