apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-brnch-count-gh.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-brnch-count-gh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-brnch-count-gh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
